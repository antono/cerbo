## 1. Vault selector flow

- [ ] 1.1 Add layout/store state for a vault selector dialog and wire `Ctrl+Shift+O` to open it.
- [ ] 1.2 Implement a vault selector UI modeled after the existing page selector, including vault list selection and add-vault action.
- [ ] 1.3 Remove the old inline vault switcher from the sidebar header and route the no-vault/add-vault entry point to the new selector.

## 2. Hotkeys help

- [ ] 2.1 Add a help icon near the theme toggle that opens keyboard shortcuts help.
- [ ] 2.2 Update keyboard shortcuts help text to include the vault selector shortcut and keep the existing shortcut list accurate.
- [ ] 2.3 Verify Escape and modal-closing behavior works consistently across the new selector and help surfaces.
